# 驗證紀錄

驗證日期：2026-09-07。環境：macOS、Python 3.14.4；直接依賴版本已固定於 requirements.txt。

## 換機接續驗證

2026-09-07 晚間，於目前 macOS、Python 3.13.5 環境重新驗證：

- 資料夾內未附 `.venv`，已依原有 `requirements.txt` 建立獨立虛擬環境並完成安裝，無須修改套件版本。
- ２３項 unittest／Streamlit AppTest 測試全部通過；`pip check` 未發現相依衝突。
- 解除執行沙盒的網路限制後，`scripts/check_live.py` 完成台積電日線、分時、週線、月線、美股、自選、比較及市場／事件頁流程，回報 `LIVE FLOW OK`，各頁沒有程式例外。
- 以 `./start.sh` 啟動本機服務，Chrome 實際確認首頁的台灣大盤行情摘要與技術圖表載入。
- 本次接續時沒有 `data/preferences.json`，使用預設清單與設定；未從原電腦還原個人偏好。

以下保留原環境的驗證紀錄；本次未重新操作瀏覽器下載或測試全部螢幕尺寸。

## 自動測試

- ２３項 unittest／Streamlit AppTest 測試通過。
- 包含跨時區交易日、昨收與開盤價區分、收盤競價時間優先、Yahoo 延遲載入 metadata 的快取序列化、空資料與舊資料備援、共同交易日報酬、短歷史指標、設定驗證及原子存檔、四頁操作與設定保留。
- `pip check` 未發現相依衝突；`git diff --check` 通過。

## 真實資料與頁面流程

使用 `scripts/check_live.py` 透過真實 Yahoo API 驗證：

- 台積電日行情、最近交易日分時、週線與月線均載入，沒有頁面例外。
- 蘋果美股Ｋ線與分時載入，非當地當天資料有明確日期提示。
- 自選清單與多標的比較成功載入。
- 市場指數、匯率、蘋果財經事件及新聞成功取得。

此腳本執行 Streamlit 控制項，但不存入使用者設定；可用 `.venv/bin/python scripts/check_live.py` 重跑。這項檢查需要網路，Yahoo 暫時失敗時可能不通過。

## 瀏覽器檢查

使用 Chrome 訪客視窗操作本機伺服器，已檢查：

- 深色首頁、行情摘要、Ｋ線／成交量／MACD／KD 的垂直排列與日期軸。
- 自選總覽載入、點選台積電列導向個股、伺服器重啟後仍記住所選標的。
- 比較頁的共同期間、曲線及報酬表，並修正首次掛載控制項時，前端預設值與計算採用值不同步的問題。
- 市場指數、匯率、台積電事件及新聞資料載入。

## 界限

行情來源的延遲、缺少分時分鐘、事件缺漏與日曆預估仍依 Yahoo 而定。未執行交易，未驗證所有清單標的、所有螢幕尺寸或瀏覽器。PNG／CSV 已接上下載功能，未把瀏覽器下載檔案寫入使用者的下載資料夾。
